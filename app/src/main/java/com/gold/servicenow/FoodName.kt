package com.gold.servicenow

class FoodName (imageId: Int, name: String, price: Float) {
    var imageId = imageId
        private set

    var name = name
        private set

    var price = price
        private set

}